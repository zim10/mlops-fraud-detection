# .github/workflows/deploy.yml
name: Deploy MLOps Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  AWS_REGION: ap-southeast-1
  PULUMI_STACK: <your-pulumi-username>/mlops-pipeline/sandbox

jobs:
  test: ...
  deploy-infrastructure: ...
  build-and-push: ...
  deploy-services: ...